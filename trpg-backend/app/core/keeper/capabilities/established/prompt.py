"""established 的 prompt 块。

🔴 **2026-08-23 第二次真机调**（`exec/48` A3）：351 拍的局里 `new_facts`
**296 次裁决 0 条**，而 `new_threads` 写了 10 条——不是模型不配合，是**这张
清单按设计就不覆盖那一类**。探针问"借书卡夹在第几页""那盏灯的外号"，两条都
被**编了一个**（47 页实为 88、"歪脖子"实为"瘸腿老头"），语气跟答对时一样确信。

缺的是「**玩家自己创造的、带一个具体值的、低戏剧性的事实**」：记下车牌、把
借书卡夹进第 88 页、在电线杆上系红布、把钥匙埋在第三块石板下、给灯起外号
——五件事**一条都不命中**原来那六个动词。它们四层全漏：`fact_ledger` 只认模组
预定义的 fact id、这一片的判据排斥它们、L2 装不下（120 字要覆盖 12–80 拍）、
L3 会滚走。而玩家一定指望系统记得，**因为那是他自己做的**。

🔴 **2026-08-16 真机调过一轮**：第一版 11 拍里 `new_facts` 一次都没被写
（砸锁、开枪、拿走日志、烧掉房子、交给警察，全落进 threads 或干脆没记）。
三个原因都是自己造成的，一起改掉：

1. **规则标着 4h，order 却是 4.75**——排在 4f（悬而未决，4.95）**之前**。
   而这条规则通篇拿 `new_threads` 做对照，模型要三条之后才读到那个词。
   改成 4.98，紧跟在 4f/4g 后面。
2. **输出示例是空数组**，而 `new_threads` 的示例是**填满的**
   （`[{"text": "米-戈仍在追击"}]`）——模型照着看到的形状抄。改成填满的。
3. **判据太抽象**：「不可逆且后果永久」要模型自己下定义。换成**可对照的动词
   清单**（烧毁/杀死/救活/交出/永久失去/彻底翻脸）。
"""

from __future__ import annotations

from app.core.keeper.contract.registry import PromptBlock

_RULE = PromptBlock(
    slot="rules",
    # 🔴 必须排在 4f（悬而未决）之后：这条规则是拿它做对照定义的。
    order=4.98,
    text=(
        "4h. **已经成为定局的事**：写进 `new_facts`："
        '`[{"text": "调查员烧掉了科比特家的地下室"}]`，系统会分配一个 `fact-N`。\n'
        "   **对照下面这张清单，命中就要写**——不用自己判断"
        "「算不算不可逆」：\n"
        "   · 烧毁 / 炸毁 / 砸毁了一个地方或一件重要东西\n"
        "   · 杀死、重伤或救活了一个人（含怪物）\n"
        "   · 把关键证物**交给了**别人（警察、委托人、NPC）\n"
        "   · 永久失去了一件重要东西（弄丢、被夺走、烧掉）\n"
        "   · 跟某个 NPC 彻底翻脸、或者达成了不会反悔的约定\n"
        "   · 揭穿了某个人的身份、或者让某件事公之于众\n"
        "   🔴 **跟 `new_threads` 的区别是「还要不要你继续演」**：追兵还在后面、"
        "火还在烧、门被反锁着——那些**还没完**，写 new_threads，之后用 "
        "`resolved_threads` 结清；**已经做完、结果定了**的写这里，它没有结清动作、"
        "会一直留着。\n"
        "   🔴 **结清一条 thread 的时候顺手想一下**：它了结之后留下了什么永久的"
        "后果吗？火烧完了 → 「那栋房子已经烧毁」；追兵甩掉了 → 通常什么也不留。"
        "留下了就补一条 `new_facts`。\n"
        "   🔴 **局面块「已经成为定局的事」里已经列出来的，不要再记一遍**——"
        "那件事已经有 `fact-N` 了。同一件事换个说法再写一条，表里就会并存两条"
        "指向同一件事的记录，而这一片没有结清动作、清不掉。\n"
        "   🔴 **不要拿它记这些**：位置、线索、随身物品、谁在台上、疯狂、生命值"
        "——系统都已经记账并摆在局面块里了。也不要记只影响这一段叙事的细节"
        "（那种直接写进 narration_guidance）。\n"
        "   \n"
        "   🔴 **还有一类也要写进来：玩家自己做的、带一个具体值的小事。**"
        "上面那张清单说的是「世界发生了不可逆的变化」，而这一类不改变世界，"
        "**只在玩家的账上多了一条他以后会问起的东西**：\n"
        "   · 把某样东西**藏在 / 放在 / 埋在**某个具体位置\n"
        "   · 在某处**留下记号**（刻痕、系带子、做标记）\n"
        "   · **记下**一个具体的数字或名字（车牌、门牌、电话、页码、日期）\n"
        "   · 给某个东西或某个人**起了名字或外号**\n"
        "   · 跟某人**约定**了具体的时间或地点\n"
        "   🔴 **判据是「有没有一个具体的值」**：「他把钥匙埋在门廊右边第三块"
        "石板下」要写（值 = 那个位置）；「他在院子里找了一圈」不写（没有值）。\n"
        "   🔴 **写的是他做了什么，不是现在什么状态**：钥匙后来被人挖走了，"
        "「他当时把钥匙埋在那儿」这件事**仍然为真**——这一片记的是发生过的事，"
        "所以它不需要、也没有结清动作。后来的变化另写一条。\n"
        "   🔴 **不写这些**：只是看了 / 听了 / 闻了 / 搜了一遍（没有创造任何"
        "具体的值）· 走到哪儿去了（位置有自己的账本）· 玩家的猜测与心情。"
    ),
)

#: 🔴 **填满的示例**，不是空数组——见模块 docstring 第 2 条。
_EXAMPLE = PromptBlock(
    slot="output_example",
    order=76,
    #: 两条：一条不可逆定局，一条"玩家自己做的带具体值的小事"。
    #: 🔴 **示例必须两类都有**——`exec/41` 那次的三个原因之一正是"示例是空数组
    #: 而 threads 的是填满的，模型照形状抄"。只给一类示例，模型也只会记那一类
    #: （296 次裁决 0 条 new_facts，而清单里那六个动词一个都没命中过）。
    text=(
        '  "new_facts": [{"text": "调查员烧掉了科比特家的地下室"}, '
        '{"text": "调查员把备用钥匙埋在门廊右边第三块石板下"}]'
    ),
)

PROMPT_BLOCKS = (_RULE, _EXAMPLE)
