"""COC7 规则权威：规则的**定义**与**裁决**都在这里。

🔴 项目红线：**规则权威在后端**。前端只做即时反馈的"预演"，而预演用的每个
参数都必须来自这里——不许在前端硬编码任何规则数字。

- `content.py` —— 规则数据（技能表、职业表、属性表）。随发版进来，不进数据库；
- `rules.py` —— 规则引擎（技能基础值公式、点数校验、衍生值）；
- `age.py` —— 年龄修正表。

⚠️ 规则问题以 `rules/COC7空白卡CY23Final.xlsx` 的「职业列表」/「更新说明」
两个 sheet 为准（含 Chaosium 官方裁定）。有三件事**已核实是正确行为、不要"修"**：
`occupation_spent` 记账为负、信用评级超出下限走兴趣点、480 属性点上限。
"""
