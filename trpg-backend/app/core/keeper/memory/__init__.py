"""三层记忆。判据：**"必须记住"的走 L1，"记得大概就行"走 L2。**

| 层 | 文件 | 是什么 | 会不会滑走 |
|---|---|---|---|
| L1 | `fact_ledger.py` | 事实账本，**代码记账** | 不会（不设 limit） |
| L2 | `chapter.py` | 分段摘要，LLM 离线压缩 | 不会 |
| L3 | `history.py` | 最近 200 条滑动窗口 | **会** |

L1/L2 都不设 limit——**活过 L3 的窗口正是它们存在的理由**。一场几十小时的战役
有上千轮，200 条只覆盖最近 50–80 轮，而滑走的恰恰是委托怎么下的、最早的证词
这些最需要记住的部分。

L1 与 L3 都带**受众**：分头时另一边的线索与历史不进这一组的上下文
（`exec/14 P5.2d`）。
"""
