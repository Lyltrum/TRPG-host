# 11-待办:叙事纪律修复的泛化问题

> 2026-07-28 讨论记录。暂不实施,先记录待办。

## 起因

前两轮真人实测(科比特先生×2、神秘渡轮×1)修的几处叙事纪律 bug,按"是否结构性泛化"分两类:

- **结构性修复(天然覆盖所有模组)**:60s 请求超时、开场仪式改走 LLM 叙事而非原文粘贴、story 页面只展示 `player_intro`。改的是代码路径,跟具体模组内容无关。
- **样本驱动的模式匹配(泛化边界不可知)**:技能同义词字典(`观察/闪躲/侦查`→`侦察/闪避`)、机制播报泄露正则、伪造检定记账泄露正则。这几条是"见一个具体失败样本→补一条规则堵住它",本质是打地鼠——换个模组、模型换个措辞,现有规则大概率堵不住。

已核实:`trpg-backend/app/core/keeper/tools.py:251` 的 `roll_check` 工具,`skill_name` 参数在 schema 层是自由文本 `str`,没有 enum/校验约束,LLM 想传什么词都行——这是同义词字典会持续缺条目的根因。

## 待办 1:技能名解析改模糊匹配 —— ✅ 已用另一条思路彻底解决(2026-07-30)

- **现状(修复前)**:`_resolve_skill_target`(`tools.py:162`)精确匹配 + 人工维护的 `_SKILL_SYNONYMS` 字典 + 复合名后缀匹配。
- **弯路**:先评估了字符串层面的模糊匹配——查证三个已知案例(观察→侦察、侦查→侦察、闪躲→闪避)后发现**都是近音/同义说法,不是打字错误**("侦查"的"查"和"侦察"的"察"读音完全相同,都是 chá),字符编辑距离(difflib/Levenshtein)接不住这类"同音不同字",拼音匹配需要新依赖且覆盖率也有限。用户当场判断这条路"没有意义,不能完全解决这类问题",三个字符串匹配方向都被否掉,并要求"不要只是记下来,你要给我解决这个问题"、"排查一下"是不是已经有类似机制解决过。
- **真正的根因**:回去重新读 `prompts.py::build_adjudicator_instructions` 才发现——**"侦查"根本不是模型编出来的同义词,是我们自己的裁决器 system prompt 里三处硬编码写错了**:规则1的示例"搜索房间→侦查"、输出格式 JSON 示例 `"skill": "侦查"`、结尾提示"技能/属性用中文名(侦查、图书馆使用……)"——而 `coc7_content.py` 里这个技能的真实规范名是"侦察"(id `spot-hidden`)。模型只是老实执行了我们自己教给它的错误示例,根本不需要任何"识别同义词"的能力。
- **修复**(不是模糊匹配,是给模型一份权威参照 + 改掉自己写错的示例,跟 NPC 表"专有名词以此为准"同一个模式):
  1. 改掉 prompt 里三处硬编码的"侦查",统一改成"侦察"。
  2. 新增 `render_skill_reference(ruleset)`,把 `ruleset.skills`/`ruleset.attributes` 的全部权威名称渲染成一张表,常驻进裁决器 prompt(仿 NPC 表的"专有名词以此为准,不得另起名字"),并加一条规则"技能/属性名必须原样取自上表,禁止用同义词/口语说法"——模型自己的中文语感能挑对同音同义词,不需要事后靠字符串匹配去猜。
- **验证**:新增 `tests/test_prompts.py`(3 条,含专门守住"侦查不能再出现在规则示例/JSON示例/结尾提示"这三处的回归测试),`pytest` 316 全绿,`ruff check`/`ruff format --check`/`ty check` 均过。改动落点:`prompts.py`(新函数 + 签名加 `ruleset` 参数 + 三处文案修正)、`agent.py`(调用处传 `ruleset`),未碰协议/DTO/SDK/前端。
- **教训**:被要求"重新排查"这件事本身值得记——第一轮分析(字符串模糊匹配三方案)是在**没有先去读实际 prompt 内容**的情况下做的,直接假设"模型自由发挥导致同义词漂移",而没有去验证"prompt 有没有教错"这个更基础的可能性。用户没有指定具体修法,只是说"方案很差、去排查",逼出了正确的根因。

## 待办 2:排查伪造检定记账泄露的根源 —— ✅ 已确认并修复(2026-07-30)

- **现状(修复前)**:`prose_discipline.py` 新增了 `_FAKE_STAT_LOG_LEAK` 正则,事后从叙事正文里砍掉形如 `[理智] xxx → 损失 2,当前 San 52` 的伪造记账句(真人实测 10-#5,数据库核实这类数值从未真实发生过)。
- **根因已确认**:`agent.py::_load_room_memory` 把 `keeper.check`/`keeper.san`/`keeper.hp` 三类历史事件格式化成 `[检定] 阿福 侦察：30/70 → 成功`、`[理智] xxx：损失 N，当前 San N`、`[生命] xxx：N（原因），当前 HP N` 这种方括号"记账行",拼进 `situation`(`format_turn_input` 的"游戏历史"块),这段文本会被裁决 LLM **和**叙事 LLM(temperature 0.8,自由写作)同时看到——叙事阶段照猫画虎模仿这个可逐字复刻的模板,编出格式神似但数值全假的记账句。
- **修复**:把这三类事件的历史行渲染从方括号记账格式改成普通叙述句(不带任何方括号/特殊标记的自然语言),如"阿福进行了一次侦察检定，掷出30，目标70，结果成功。"——裁决 LLM 依然能从自然语言里读懂"末尾是否有未叙述的检定结果"(裁决规则7 的措辞已同步去掉对字面 `[检定]/[理智]` 标签的引用,改成描述性说法),但不再存在一个"看起来像系统记录、可以被逐字模仿"的模板。`prose_discipline.py` 的 `_FAKE_STAT_LOG_LEAK` 正则保留作为兜底安全网(不影响,只是预期不再触发)。
- **验证**:`tests/test_keeper_agent.py::test_load_room_memory_replays_events_in_order` 更新断言;`pytest`(305 全绿)、`ruff check`/`ruff format --check`/`ty check` 均过。改动落点仅 `agent.py::_load_room_memory` 三个 `lines.append` + `prompts.py` 裁决规则7 一处措辞,未碰协议/DTO/SDK/前端。

## 优先级

用户判断:这两条可以做,但不是当前最优先——当前更想先看"车卡选择"独立前端项目能否把优点移植进主前后端(见对话记录,尚待用户提供该项目路径)。这两条记为待办,后续排期时再拿出来。

**2026-07-30 补充**:待办1、待办2 均已在后续排期中完成并验证,见各自小节。本文件里"样本驱动模式匹配泛化边界不可知"这个诊断框架整体上依然成立(机制播报泄露正则等仍是打地鼠式修复),但待办1 的实际根因比诊断时设想的更基础——不是"泛化边界不可知"的模式匹配问题,是 prompt 自己教错了名字,修法也因此不是"更好的模式匹配"而是"给模型权威参照 + 改对自己的示例"。
