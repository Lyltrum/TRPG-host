# 12 · 真人实测问题清单(追书人,持续增量记录)

> 2026-07-29,建卡向导重制（redesign-v2）落地后，用户在浏览器里用重制后的
> 建卡向导建完卡、正式进入「追书人」模组游戏时顺带发现的问题——跟建卡向导
> 重制本身无关，是游戏内主持人开场体验的缺口。这份文件只做**记录**，先把
> 问题攒齐，再统一改，不要每条单独改。
>
> 环境：后端 `127.0.0.1:8000`，前端 `127.0.0.1:9877`，真实 DeepSeek。
>
> 每条记录：症状（用户描述）→ 诊断（读代码后的根因）→ 候选修法 → 状态。

## 问题记录

### #10「完成」步：校验时机太晚 + 不该有独立的「导出角色卡」界面

- **界面**：建卡向导重制版第 8 步「完成」（截图：摘要区显示八维属性/
  LUCK + 衍生值 HP/SAN/MP/DB/MOV 全部为 0；校验区显示红色错误"属性点
  总数 493 超出预算 480"）。
- **症状/诉求**（用户原话拆分为两点）：
  1. **校验时机太晚**：预算超支这类校验不应该等到走到最后「完成」这一
     步才提示，应该在用户进行属性/技能分配的**当下**（第 2 步属性分配、
     第 5/6 步技能加点等）就实时校验、当场提示，而不是让用户一路走到底
     才被告知"超出预算"。
  2. **不该有独立的「导出角色卡」界面/流程**：完成步骤里现在的定位是
     "确认摘要 + 导出一份角色卡备份"，用户认为这不需要作为一个面向用户
     的导出功能——角色卡信息应该由后端在完成建卡时默认直接存进数据库，
     让 agent（守秘人）能读到就行了，不需要用户手动"导出"这个动作/界面。
  - **附带证据**：本次截图里衍生值 HP/SAN/MP/DB/MOV 全部显示 0，且
    属性点已超预算（493/480）——衍生值为 0 与 #4 记录的"平均分配没有
    联动衍生属性显示"是同一类现象的又一次复现，可作为 #4 的佐证，不
    单独开新问题。
- **诊断**：待读代码确认（`FinishStep.tsx` 的校验触发时机 vs 各步骤
  `stepBlockers`/实时预算条的校验覆盖范围是否本来就该在每步做但漏了；
  `ExportPanel.tsx` 当前导出功能的实际用途——是否本来就该在 `complete`
  提交成功后自动完成、这个面板只是多余的手动步骤）。
- **候选修法**：待定。
- **状态**：已修复（未提交）。校验时机：`CharacterWizardPage.tsx` footer
  新增贯穿除完成页外所有步骤的校验提示条。导出面板：`FinishStep.tsx`
  删除整个"导出角色卡"区块 + `ExportPanel.tsx` 组件（完成创建本来就会
  自动 `saveCharacter`+`completeCharacter` 存库，供 agent 读取）。详见
  `docs/character-build-migration/wizard-bugfix-round1.md`。

### #9「兴趣技能」加点步：疑似与 #8 同样的问题，需要同步核查/修

- **界面**：建卡向导重制版第 6 步「兴趣技能」（`InterestPointsStep.tsx`，
  未截图）。
- **症状/诉求**（用户原话）：兴趣技能的界面也需要做相关的修改——即
  #8 记录的两个问题（技能基础值疑似被忽略、加点后顶部预算条不更新）
  大概率在"兴趣技能"这一步同样存在，需要同步核查修复。
- **诊断**：待读代码确认（`InterestPointsStep.tsx` 是否和
  `OccupationPointsStep.tsx` 共用同一套预算条/`SkillRow` 组件，若是则
  #8 的根因修好后这里应该一并修好；若两步各自独立实现，需要分别核查）。
- **候选修法**：待定，预计随 #8 一并处理。
- **状态**：已修复（未提交）。跟 #8 同一个根因，见 #8 的状态记录；另外
  `InterestPointsStep.tsx` 也补上了 `previewError`/属性未校验的提示。

### #8「职业技能」加点步：基础值疑似被忽略 + 加点后预算条不更新

- **界面**：建卡向导重制版第 5 步「职业技能」（演员-戏剧演员，职业技能点
  公式 EDU*2+APP*2；截图：顶部「职业技能点」预算条显示 `0/—，还剩 0 点`；
  信用评级 15（范围 9-40）；下方技能列表格斗:斗殴 25、射击:手枪 20、
  话术 5、恐吓 15、心理学 10、乔装 5、历史 5，格斗/射击/话术/恐吓四项
  带"槽"标记）。
- **症状/诉求**（用户原话拆分为两点，原话有语音转写误字，按语义整理）：
  1. **技能不花点数就自带的基础值疑似被忽略**：像格斗:斗殴 25%、射击:
     手枪 20% 这类"不需要分配属性点就已经有的基础数字"，在当前的加点/
     预算计算里似乎没有被正确考虑进去（用户原话："把前面的不用分配属性
     点就已经有的基础数字，给忽略了"）。
  2. **加点后顶部预算条数值没有变化**：用户描述自己确实点了 +/- 做了
     加点操作，但最上面的「职业技能点 (EDU*2+APP*2)」预算条（`0/—，
     还剩 0 点`）没有跟着变化，看起来是没有联动更新。
- **诊断**（已确认根因）：不是 `OccupationPointsStep.tsx`/`SkillRow.tsx`
  自己的问题——`compute_character_preview()`（`app/service/character.py`）
  调用 `compute_preview()` 时一直没传 `generation_method`/
  `attribute_pool_total`，预览请求永远按点数购买法（预算 480）校验属性
  总和；掷点池玩家的池子总值几乎从不是 480，几乎必然被误判为超预算，
  `_compute()` 属性校验失败时整体短路，`derived_stats`/两个技能点预算/
  `skill_view` 全部退化成空——这正是"0/—"和"基础值显示成原始 25/20/5…
  却加不上去"的真实来源。
- **候选修法**：见状态。
- **状态**：已修复（未提交）。后端补 `generation_method`/
  `attribute_pool_total` 透传 + 前端 `useWizardPreview.ts` 同步发送这两个
  字段；另外给 `OccupationPointsStep.tsx` 加了 `previewError`/属性未校验
  时的解释文字（防御性 UX，即使以后再出现类似异常也不会静默显示 0）。
  详见 `docs/character-build-migration/wizard-bugfix-round1.md`。

### #7「自选技能槽」下拉框应允许留空，不该是必选

- **界面**：建卡向导重制版第 4 步「选职业」详情区的「自选技能槽」
  （截图：演员-戏剧演员职业，四个槽——格斗任选一项专精/两项社交技能
  取悦话术恐吓说服/任意一项其他个人或时代特长/技艺（表演）——前三个
  已选中具体技能，最后一个"技艺（表演）"显示"— 请选择 —"占位）。
- **症状/诉求**（用户原话）：自选技能槽的选择应该允许为空，需要增加
  "留空"这个选项，因为这个不是必选的。
- **诊断**：待读代码确认（`ChoiceSlotPicker.tsx` 当前是否强制要求每个
  槽都选中某个候选技能才算"选满"，`stepBlockers`/`wizard-state.ts` 里
  「下一步」按钮的可用性判定是否把"自选槽全部选中"当成硬性前提；这跟
  #6 里页面自己的提示文案"带自选槽的职业选完槽后才算选满"是否矛盾，
  需要先确认用户想要的是"槽可以不选、职业技能点对应的份额就不领"这种
  语义，还是别的含义）。
- **候选修法**：待定。
- **状态**：已修复（未提交）。`wizard-selectors.ts::stepBlockers` 的
  `'occupation'` case 去掉"槽位必须选满"的校验，只保留"必须选了职业"；
  `slotPicks` 本来就只是前端本地模拟，从不提交后端，留空不影响后端的
  自动占槽计算。

### #6「选职业」步：应按类别分类，不能只显示前 40 个

- **界面**：建卡向导重制版第 4 步「选职业」（截图：只有一个搜索框 +
  平铺列表，显示会计师/杂技演员/演员-戏剧演员/演员-电影演员/事务所侦探
  保安/精神病医生（古典）共 6 项，底部提示"已显示 40/229，请搜索缩小
  范围"）。
- **症状/诉求**（用户原话）：应该先把 229 个职业按类别分类展示，而不是
  只搜索/平铺列表；当前"只显示 40 个、要打字搜索才能缩小范围"这种方式
  没法精准定位到每一个职业——应该能通过分类导航精准找到任意一个职业。
- **诊断**：待读代码确认（`OccupationStep.tsx`/`OccupationListItem.tsx`
  目前是否只有搜索框+固定截断 40 条的列表，是否已有职业分类字段/数据
  可用于分组，此前 `data/occupations.ts` 被删除时是否连带丢失了分类
  信息，还是分类数据从未存在过、需要新增）。
- **候选修法**：待定。
- **状态**：已修复（未提交）。后端 `OccupationSpec` 新增 `category` 字段，
  229 个职业逐条分类（12 类）；前端加分类 tab + 去掉 40 条硬截断，任意
  职业都能通过分类+搜索精确找到。

### #5「年龄」步：年龄输入框行为不明确 + 对照表应可折叠

- **界面**：建卡向导重制版第 3 步「年龄」（截图：当前年龄默认 28，下方
  一张完整的年龄档对照表 15-19～80-89，底部「重新套用」按钮 + 「调整
  结果」区块）。
- **症状/诉求**（用户原话拆分为两点）：
  1. **年龄修改的联动行为需要明确**：用户描述的预期是"每当修改（年龄）
     时，它会自动按照年龄档对照表对应相应的年龄，并同时进行基础属性点
     的修改"——即改年龄应该是**改完立即自动生效**（页面文案里其实已经
     写了"改了年龄会立即重新套用"），但当前界面还有一个独立的「重新
     套用」按钮，跟"改了就立即生效"这句文案矛盾，容易让人以为改了年龄
     数字之后还要额外点一下按钮才会生效（用户没提到自己点没点这个按钮，
     但从描述看这个"自动生效"的心智模型和界面实际交互对不上）。
  2. **年龄档对照表占用空间太大，应该做成可折叠**：截图里完整表格
     （15-19～80-89 共 7 档 × 3 列）常驻展示，用户认为这只是参考信息，
     不需要一直占屏幕，应该收起来、需要时再展开看。
- **诊断**（已确认）：`commitAge()`（失焦/回车）本来就会自动调用
  `applyNow()` 套用，逻辑没问题，问题在呈现——"重新套用"按钮容易被误读
  成"要点它才生效"，实际是"同年龄重新掷一次 EDU 改进检定"这件完全不同
  的事。
- **候选修法**：见状态。
- **状态**：已修复（未提交）。按钮改名"🎲 同年龄重掷教育检定（结果可能
  变差）"消除歧义，年龄输入框下方加"✓ 已套用"确认文案；年龄档对照表
  改成默认收起的可折叠区块。

### #4 「属性与幸运」步：生成方式简化 + UI 太大 + 平均分配没联动衍生属性 + 掷骰应限一次

- **界面**：建卡向导重制版第 2 步「属性与幸运」（截图：掷点池模式，
  已掷出总点数 490，八项属性逐个手动分配，力量当前 60）。
- **症状/诉求**（用户原话拆分为四点）：
  1. **生成方式简化**：不需要「点数购买」「服务端掷骰」「掷点池」三选一，
     直接用投骰子的方式决定总点数分配就够了——「点数购买」「服务端掷骰」
     两种可以废弃，只留掷骰（掷点池）这一种。
  2. **单项属性卡片 UI 太大**：每个属性（如截图里的"力量 STR"卡片，含
     -/数值/+ 大按钮 + 困难/极难提示 + 40/50/60/70 四个预设值按钮）占的
     竖向空间太多，八项属性要滚动很久，不需要这么大。
  3. **平均分配没有联动衍生属性**：点了「平均分配」按钮后，八项属性数值
     变了，但衍生属性（HP/SAN/MP/DB/MOV 等）看起来没有跟着重新计算/显示
     更新（本轮实测：属性总点数 bar 从 180 变化，但用户观察不到衍生值
     跟着刷新）。
  4. **属性掷骰、幸运掷骰都应该只允许掷一次**：现在能反复点「服务端掷骰」
     /「掷点池」/幸运骰子重新摇出新的点数组合（截图里能看到 8 组候选
     掷骰结果可选），这不符合"掷一次定终身"的规则预期。
- **诊断**（已确认）：③的根因跟 #8/#9 是同一个（`compute_preview` 生成
  方法透传缺失）。①②④是纯 UI/流程改动。
- **候选修法**：见状态。
- **状态**：已修复（未提交）。①新建角色默认且唯一走掷点池（点数购买/
  服务端掷骰从 UI 移除，老角色卡仍保留手动编辑但不提供切换/重掷）；
  ②`AttributeAllocCard.tsx` 压缩到约 65% 高度；③后端+前端一起修好
  `generation_method` 透传后自然恢复联动；④属性池/幸运各自加了服务端
  "只能掷一次"的守卫（第二次调用 409 `ALREADY_ROLLED`），前端也不再
  提供重掷入口。

### #3 建卡向导「基本信息」步不该给性别/居住地/出生地预填默认值

- **界面**：建卡向导重制版（redesign-v2）第 1 步「基本信息」。
- **症状**（用户描述）：性别默认选中「男」、居住地默认填「阿卡姆」、出生地
  默认填「阿卡姆」，这些不该是默认强制选中/填入的。
- **用户判定的正确行为**：
  1. **强制填写**：角色姓名、性别。
  2. **允许留空**：居住地、出生地。
  - 换句话说，问题不是"这两类字段该不该必填"分错了（性别本来就该强制
    选择，这点当前实现是对的），而是**性别不该有预选中的默认值**（应该
    要求玩家主动选择,比如初始不选中任何一项)，**居住地/出生地不该有
    预填内容**（应该允许留空,不能拿"阿卡姆"当默认值替玩家做了决定）。
- **诊断**（已确认）：默认值来自 `wizard-state.ts::createInitialWizardState()`。
- **候选修法**：见状态。
- **状态**：已修复（未提交）。三个字段默认值改成空字符串；性别 `<select>`
  加占位选项强制主动选择；`stepBlockers` 同步补上性别必填校验。

### #2（追加）agent 读到的角色卡不含结构化背景故事 8 字段 —— 已修复

- **背景**：用户追问"agent 能不能拿到我的建卡信息、跟以前格式有没有区别"，
  借此核实了 `get_character_sheet_impl`（`app/core/keeper/tools.py`）的
  实现——角色核心字段（属性/衍生值/已训练技能/自由文本 `background`）跟
  建卡向导重制前完全一样，数据库表结构和提交接口这次都没动。但顺带发现
  这次建卡新增的结构化背景故事 8 字段（`background_detail`）**从未被
  agent 读取过**，只读了旧的自由文本 `background`。
- **用户判定**：需要修。
- **修复**：`get_character_sheet_impl` 新增 `_BACKGROUND_DETAIL_LABELS`
  中文标签映射（跟前端 `character-model.ts::BACKGROUND_DETAIL_FIELDS`
  两边各自维护，后端仍把这个字段当透明存取，这里只是展示层解释）；只列
  玩家真的填过的字段（跟"已训练技能"同样的降噪原则，全空不出现"背景
  细节："这行）。补了两条测试（有内容/全空两种情况），pytest 278 全绿。
- **状态**：已修复（未提交）。

### #1 建完卡进入游戏房间后，直接被守秘人开场白"砸脸"，没有持久可查的案件简报

- **模组**：追书人
- **症状**（用户原话）：建完卡进入游戏后"直接就进入了这个界面……我只在
  初始界面知道了大概的走向，但是进来之后他直接就来这么一段话，我很懵逼"。
  截图显示：顶部 `案件档案已加载 · 向守秘人描述你的行动即可推进` 状态条 →
  `守秘人正在开场...` → 一大段守秘人第一人称叙事（委托人托马斯·金博尔的
  陈述），底部是角色卡/技能/地图/笔记四个 tab。
- **诊断**：
  1. **不是 bug，是既定流程**：建卡完成 → 进房间 → 守秘人开场叙事，这条
     链路（`RoomPage.tsx` 里回补开场叙事的 `useEffect`、后端
     `_run_opening_ceremony`/`narration.push` 回放）跑得完全正常，没有
     缺失的中间步骤，也是这个项目已经用其它模组（科比特先生、神秘渡轮）
     反复真人实测验证过的路径。
  2. **真实缺口**：委托简介（`player_intro`，即"追查偷书贼、追回书籍、
     查清叔叔死活"这段任务描述）**只在建卡之前的 `StoryPage`（剧情介绍页）
     出现一次**，进了游戏房间之后就再也看不到、也无处可查——底部四个 tab
     里，"笔记"（`sheetPage`/`速记本`，`RoomPage.tsx:947,1260`）只是一个
     空白自由文本框（placeholder 是"📋 案件笔记"），不是结构化的任务/案件
     信息展示；`地图`/`技能`/`角色卡`三个 tab 也都不含 `player_intro`。
     玩家一旦错过或忘记 `StoryPage` 上看过的那段简介，游戏内**没有任何
     地方能让他确认"我的任务到底是什么"**，只能被动接收守秘人的沉浸式
     叙事文本、或者往上翻聊天记录自己找。这大概率是"很懵逼"的真正根因——
     不是叙事写得不好，是缺一个可以随时回看的任务锚点。
- **候选修法**（未选定，待用户拍板）：
  - 把 `player_intro` 固定展示在某个已有 tab 里（比如"笔记"tab 顶部常驻
    一块只读的"案件简报"区域，下面才是玩家自己的自由记事）；
  - 或者在聊天区顶部加一条可展开/常驻的"案件卡片"，点开显示 `player_intro`；
  - 或者两者结合。具体交互形式需要跟用户讨论确定，不要自己拍板选一种。
- **状态**：待处理（用户已明确表示先记录，不在本轮立即修）。
