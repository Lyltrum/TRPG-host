# 27：架构重构 —— 让这个项目可被团队承接

> 2026-08-01 立项。**当前最高优先级，其余待办（exec/26 的语音/模组导入/叙事呈现、
> 前端 UI v2）全部排在它之后。**
>
> 起因：用户指出「我一个人做」是当前**工作方式**，不是**架构目标**。这个项目
> 应该能被一个团队承接并合理分工，而这是产品的**非功能性属性**（同可测试性、
> 可观测性），必须内建，不能等人来了再改造。

## 一、判据

单一判据，所有设计决策都对着它检验：

> 🔴 **一个新人能不能只读一个目录，就完成一个功能——而不必理解整条链。**

现在的答案是**不能**：加一个守秘人能力，他得同时读懂并修改
`decision.py`(schema) + `prompts.py`(教模型) + `tools.py`(执行) + `agent.py`(接线)
四个文件，还得知道它们为什么必须一起改。

两条支撑理由（用户提出，已采纳）：

1. **等到有人来了再改就改不动了。** 架构债是复利的：`agent.py` 现在被 46/90 个
   功能碰过，再长半年可能是 60/120，那时拆它的风险与工作量不是一个量级。
2. **这是自我实现的预言。** 个人项目形态的架构**本身就是接手门槛**——没人能快速
   上手，于是永远只能一个人做，于是永远"不需要"为多人设计。循环必须从架构这头断。

## 二、现状测量（不是印象，是数据）

### 依赖结构：健康，只有一条坏边

```
循环依赖 5 组，全部经过同一条反向边：core.narrator → core.keeper.agent
移除它之后：0 组
```

`keeper/` 内部本来就是干净的 6 层 DAG：

```
L0  catalog chapter decision dice history module_loader pending phase
    prose_discipline scene_state sheet_digest skill_names
L1  agenda_state check_guard fact_ledger leak_guard location_state
    npc_state prompts subject visibility
L2  tools    L3  turn_executor    L4  agent    L5  heartbeat
```

**层次是清晰单向的，问题是目录一个字都没表达它**——25 个文件平铺。

`narrator.py` 现在同时是抽象层（`Narrator` ABC + 数据契约）、工厂
（`build_narrator` 要构造 `KeeperAgent`）和一个实现（`RoomAwareKeeperNarrator`）。
抽象反向依赖具体，现在靠**三处函数内 import** 硬撑——那是绕过循环，不是消除。

### 并行开发：这才是真瓶颈

git 历史（2026-07-20 起，90 个碰后端源码的 commit）：

| 指标 | 值 |
|---|---|
| 平均一个功能改几个源文件 | **4.1**（中位数 4） |
| `core/keeper/agent.py` 被多少功能碰过 | **46 / 90** |
| `prompts.py` | 29 |
| `controller/ws.py` | 22 |
| `tools.py` | 21 |

最常一起被改的文件对：

```
25次  agent.py + prompts.py        17次  decision.py + prompts.py
18次  agent.py + tools.py          17次  ws.py + agent.py
16次  agent.py + narrator.py       15次  prompts.py + tools.py
```

**两个人随便做两个 keeper 功能，一半以上概率撞在同一个文件上。**

### 🔴 由此得出的关键结论（推翻了第一版方案）

按技术层分目录（`contract`/`rules`/`state`/`memory`/`access`…）**不会改善并行开发，
反而更糟**：一个功能天然横跨四层，按层分只会把"改一个功能跨 4 个文件"变成
"跨 6 个目录"。

活样本是 `exec/25 #59`：加一格分类，改了 `decision`(schema) + `prompts`(定义) +
`prose_discipline`(guidance) + `agent`(分支)。**这不是文件放错地方，是能力被按
技术层切碎了。**

## 三、终态：按能力垂直切

```
core/keeper/
├── contract/              全局契约（能力片段在这里被组装成一份）
│   ├── decision.py            KeeperDecision：由各能力的字段片段组装
│   ├── module.py              ScenarioModule 剧本数据
│   └── registry.py            能力注册表（四个钩子，见下）
│
├── primitives/            🔴 能力都要用的规则原语（不属于任何一个能力）
│   ├── dice.py                1d100 · 成功等级 · 大成功/大失败
│   └── skills.py              技能 id 白名单（原 skill_names）
│
├── capabilities/          🔴 一个能力一个目录，新人只读这一个
│   ├── skill_check/           技能检定
│   │   ├── schema.py              CheckRequest / OpposedTarget
│   │   ├── prompt.py              教模型什么时候发检定
│   │   ├── executor.py            create_pending_checks
│   │   ├── guard.py               check_guard（模组标注护栏）
│   │   └── test_skill_check.py
│   ├── san_check/             理智检定
│   ├── movement/              位置 · 分头 · 隐匿
│   ├── clue_reveal/           线索揭示（原 visibility）
│   ├── agenda/                议程时间轴
│   ├── health/                HP 变化（原 combat）
│   ├── world_state/           世界状态记账
│   └── progression/           对局阶段与结局
│
├── runtime/               编排（能力无关，不该随能力增长而变化）
│   ├── agent.py               主流程
│   ├── turn_executor.py       按注册表调度，不再是 if/else 长链
│   ├── pending.py             两段式待掷队列（skill/san 共用的流程机制）
│   └── heartbeat.py
│
├── memory/                三层记忆：ledger(L1) chapter(L2) history(L3)
├── access/                subject · leak_guard（view(subject)）
└── narration/             prompts 骨架 · sheet_digest · prose_discipline
```

**验收：加一个新能力 = 新增一个目录 + 在 registry 注册，不改 `runtime/` 一行。**

### 能力清单为什么是这 9 个（判据）

> **一个能力 = 一组「历史上总是必须一起改」的东西。**

用共变更验证边界：`exec/19 #38`（对抗检定）完整落在 `skill_check`；`#39`（NPC 扣血）
完整落在 `health`；`#37`/`exec/25 #60`（位置、带人移动）完整落在 `movement`。

反例同样重要：`exec/25 #59`（加 `feasibility_question` 分类）**跨不进任何能力**
——它改的是 `player_state` 分类与代码强制，那属于 `runtime/`。这条反例证明了
`runtime/` 该独立存在。

### 🔴 `checks` 为什么必须拆成三份

第一版把检定整块叫 `checks`，用户指出"名字太大"。拆开后发现根因不是名字，是它
把**共享机制**和**能力知识**混在了一起：

| 内容 | `skill_check` 用 | `san_check` 用 | 归属 |
|---|---|---|---|
| 掷骰 + 成功等级 | ✅ | ✅ | `primitives/dice.py` |
| 两段式待掷队列（`kind="skill"｜"san"`） | ✅ | ✅ | `runtime/pending.py` |
| 技能 id 白名单 | ✅ | ❌ | `primitives/skills.py` |
| `CheckRequest` schema + 护栏 + 发起 | ✅ | ❌ | `capabilities/skill_check/` |

前两行若留在 `capabilities/checks/`，`san_check` 就得 import `skill_check`——
而**阶段 0 架构测试的核心断言之一正是「能力之间不许互相 import」**，会当场变红。

> 用户凭手感说"太大"，架构测试会在代码里抓到同一件事。**手感与机器判据指向同一
> 条边界时，那条边界大概率是真的。**

### registry 的五个钩子

一个能力向系统注册五样东西（**后两个都是切 health 试点时在真代码里发现漏掉的**
——第一版设计只想到前三个）：

| 钩子 | 作用 | 例（health） |
|---|---|---|
| `schema` | 往 `KeeperDecision` 贡献字段片段 | `hp_changes: list[HpChange]` |
| `prompt` | 往裁决指令贡献一个块（带显式 `order`） | 规则 9 |
| `executor` | 往执行阶段贡献处理函数 | `adjust_hp_impl` / `adjust_npc_hp_impl` |
| **`situation`** | **往「局面块」贡献要喂给模型看的状态** | `format_npc_states`（NPC 血量） |
| **`audit`** | **往结构化日志贡献本轮该留痕的字段** | `hp_changes=2` |

第四个钩子是 `agent.py:383` 那行 `format_npc_states` 暴露的——**能力需要让模型
看见自己的状态**，否则模型不知道谁快死了。

第五个是 `agent.py` 那行 `logger.info("keeper_decision", ...)` 暴露的：它逐个
列出每个能力的字段，不抽钩子的话「加一个能力不改编排层一行」当场不成立。
而且**漏了不会报错**，只是那片能力从此在日志里隐身——线上排查时看不出它本轮
做没做事。两个钩子同一个来源：**编排层每一处"逐个列出各能力字段"的地方，
都是一个还没被发现的钩子。**

## 三之二、三处撞名一起理顺

`stealth`（技能）/ `stealth`（状态）/ `visibility`（线索）三个名字互相干扰，是
"读不懂"的一个具体来源。改名代价分层：

| 改什么 | 代价 | 结论 |
|---|---|---|
| 技能 id `stealth` | **不能改**——COC7 规则表，规则权威（`exec/17` 刚 id 化） | 保留 |
| 模块名 / 目录名 | 零 | 改 |
| `KeeperDecision` 字段名 | 改 prompt + 重录磁带 | 中，值得 |
| `ScenarioModule.visibility_pairs` | **五个模组的 structured.json 要迁移** | **不改** |

```
技能 stealth（角色卡上的能力值）   → 不动，它是规则
decision.stealth                  → hiding          「此刻藏着没」
模块/目录 visibility              → clue_reveal     「线索揭示」
decision.visibility_revealed      → clues_revealed
ScenarioModule.visibility_pairs   → 不动（模组数据，"配对表"本身不歧义）
```

改完读起来不会再混：

```python
checks: [{"skill_id": "stealth"}]              # 掷潜行检定（能力值）
hiding: [{"player": "阿铁", "hidden": true}]    # 他藏起来了（状态）
clues_revealed: ["pair-3"]                     # 线索揭开了（信息）
```

**不单独开一轮**：改名要重录磁带，跟对应能力的切分合并做，只重录一次。

## 四、障碍（都是真的，不装作不存在）

| # | 障碍 | 处理 |
|---|---|---|
| 1 | `KeeperDecision` 必须是**一份**整体 schema（LLM 的 JSON schema 不能分片） | 各能力提供字段片段，组装期用 pydantic 动态建模合并。**顺序要稳定**，否则 schema 抖动 |
| 2 | prompt 是一整段文本，且**块的顺序有语义**（规则 1–12 编号） | 注册时带显式 `order`，不靠字典序或 import 顺序 |
| 3 | 🔴 `agent.py` 里那条 if/elif 链的**顺序就是语义**（`kp_question` 优先于 `confused` 优先于…），拆错会**静默**改变行为 | **动手前先写表征测试**（characterization test）把当前分支行为逐条钉死，再拆 |
| 4 | prompt 一改，磁带 digest 全漂 | 每阶段末重录 `tests/tapes/keeper_minimal.json`；真实模组磁带本来就 gitignored |
| 5 | `tools.py` 961 行、`agent.py` 1413 行要拆散重组 | 分阶段，每阶段末系统必须完全可用 |

## 五、阶段（每阶段独立可交付，末态系统必须可用）

### 阶段 0 · 装护栏（不动业务代码）✅ 2026-08-01 已做

写 `tests/test_architecture.py`：AST 扫全部 `app/`，断言

- **无循环依赖**；
- **层级不反向**（`contract/` 不许 import 任何人；`capabilities/` 不许互相 import；
  `capabilities/` 不许 import `runtime/`）；
- 违反时报错直接指出是哪一条边。

🔴 **必须最先做**：后面每一步都靠它验证，而且**没有测试守护的架构约束一定会退化**
——今天修好，两周后一句函数内 import 就能把环带回来，且不会有任何东西变红。
同项目判据：能代码确定性判断的，一律代码强制。

### 阶段 1 · 修那条反向边 ✅ 2026-08-01 已做

`core/narrator.py` 拆成：

```
core/narration/
├── contract.py    Narrator ABC + NarrationContext/Outcome/CheckRequestNotice  ← 叶子
├── fallback.py  deepseek.py  delayed.py
├── room_aware.py  RoomAwareKeeperNarrator     ← 只有它依赖 keeper
└── factory.py     build_narrator()            ← 组装层
```

产出：循环 5→0；**`Narrator` 成为真正可插拔的接口**（新实现只对着 `contract.py` 写，
在 `factory.py` 注册，全程不碰 keeper）。纯移动，零逻辑变更。

### 阶段 0/1 的落地记录（2026-08-01）

**阶段 0** — `tests/test_architecture.py`：AST 扫全部 `app/`，三条断言（无新增
循环 / 跨层不反向 / 豁免只能变短）。扫描用 `ast.walk`，**函数体内的延迟导入照样
算**——那正是当年绕过循环用的写法。

两个当场的收获：

- **测试第一次跑就抓到一个手工分析漏掉的环**：`tools.py` 的
  `from app.core.keeper import dice` 走了包，而 `keeper/__init__.py` 里
  re-export 了 `KeeperAgent`，于是 `tools → 包 → agent → tools`。之前的临时脚本
  把 `__init__.py` 算成了不存在的 `...keeper.__init__` 节点所以漏了。
  **正式测试比临时脚本可靠。**
- **分层定义第一版是错的**：`app.core` 里混着基础设施（`db`/`errors`/`config`/
  `logging`，最底层）和领域逻辑（在 `models` 之上）。没分开时把
  `from app.core.db import Base` 误判成 6 处违规。真违规只有
  `heartbeat → service.{action_lock,ws_manager}` 两条，已列入豁免待修。

变异检验四条全部按预期变红，且**各只红对应的那一个测试**：顶层 import 新循环、
函数体内延迟导入、跨层反向、修好豁免却没删清单。

🔴 过程中踩了一次自己记过的坑：变异脚本中途 `IndexError` 退出，**复原语句没执行**，
把一个变异体留在了 `models/event.py` 里。复原必须与施加同样精确，而且脚本崩了
复原就不会跑——重写的版本用 `try/finally`。

**阶段 1** — `core/narrator.py` 拆成 `core/narration/`：

```
contract.py    Narrator ABC + 7 个数据契约   ← 实测零 app.* 依赖，真叶子
fallback.py / deepseek.py / delayed.py       ← 不碰 keeper
room_aware.py                                ← 唯一依赖 keeper 的实现
factory.py     build_narrator                ← 组装层，依赖全部实现
```

DeepSeek 的 `base_url`/`model` 常量跟着实现走（放 `deepseek.py`）——它们是那个
实现的细节，不是叙事契约的一部分。

顺带修掉包门面那条环：`keeper/__init__.py` 不再 re-export `KeeperAgent`。两个
`__init__.py` 都写了注释说明**为什么故意保持空**。

产出：**循环依赖 2 → 0，`_CYCLE_EXEMPTIONS` 清空**。改完之后棘轮主动报「以下豁免
已经不需要了，请从清单里删掉」——**不是我记得去检查，是测试提醒的**，这正是阶段 0
存在的理由。

零逻辑变更（27 个文件只改 import 路径）。685 passed + ruff/format/ty + e2e 26 全绿。

⚠️ 一个小插曲：我一度想把 `tools.py` 的包导入改成两行以"绕开包"，随即意识到没
意义——**import 子模块本来就必然先执行父包的 `__init__`**，环的根源是 `__init__`
里有 import，不是调用方的写法。已还原。

---

### 阶段 2 · 垂直切第一片（试点：**`health`**）✅ 2026-08-01 已做

本阶段真正的产出是**机制**（`registry.py`：schema 片段合并 + prompt 块排序 +
执行钩子 + 局面块钩子），后面七片照抄。

🔴 **为什么试点选 `health` 而不是 `skill_check`**（推翻了第一版）：

| 要求 | `health` |
|---|---|
| 四层齐全 | ✅ `HpChange` + 规则9 + 两个 `*_impl` + `npc_state` |
| schema 够复杂，能暴露组装问题 | ✅ 嵌套模型 + `player`/`npc` 二选一分流 |
| 能力内有分支，验证"一个能力多个 impl" | ✅ 玩家扣血 vs NPC 扣血（`exec/19 #39`） |
| **出错能被立刻发现** | ✅ 扣血算错一眼看得出；检定错了会**静默丢失** |
| 不是刚动过的核心代码 | ✅ |

`skill_check` 是最核心、最易改坏的一片（两段式掷骰 + 护栏 + 对抗 + id 白名单，
且 `exec/25` 刚动过），**第一片就切它等于在未验证的机制上动最关键的代码**。
`agenda` 则太简单（字段只是 `list[str]`，无嵌套模型），暴露不出组装问题。

**先用 `health` 跑通机制，再切 `skill_check`。**

### 阶段 2 的落地记录（2026-08-01）

落成的形状：

```
core/keeper/
├── registry.py                     四个钩子的形状（叶子：运行时零 app.* 依赖）
├── deps.py                         KeeperDeps / KeeperToolError / resolve_character …
├── primitives/npcs.py              NPC 实体寻址（health 与 world_state 共用）
└── capabilities/
    ├── __init__.py                 CAPABILITIES 元组 + 四个汇总函数
    └── health/
        ├── schema.py               HpChange + hp_changes 片段 + 权限声明
        ├── prompt.py               规则 3b + 输出示例那一行
        ├── executor.py             adjust_hp_impl / adjust_npc_hp_impl + 钩子
        ├── npc_state.py            NPC 血量的存储形态
        └── test_health_capability.py
```

骨架侧的改动全是"接口化"：`decision.py` 显式继承字段片段；`prompts.py` 的规则
清单与输出示例改成按 order 拼；`turn_executor.py` 把剩下的副作用包成带 order
的局部协程、与能力钩子**归并**；`agent.py` 那行 `format_npc_states` 换成
`situation_blocks(...)`；`subject.py` 的权限表把能力自带的那部分合并进来。

#### 验证方式：prompt 逐字节比对

本阶段硬要求**零功能变更**，最容易破的是 prompt——它是一整段中文散文，改一个
字磁带 digest 就漂。做法是切分前后各 dump 一次（用 `tests/fixtures` 的假模组，
不碰真实剧本），`diff` 必须为空。**结果确实为空，所以这一阶段没有重录磁带。**

`test_capability_registry.py` 把机制本身钉死：注册的 schema 片段必须真的被
`KeeperDecision` 继承、权限声明必须真的进 `subject` 的表、prompt 块必须出现在
成品里且规则 3b 夹在 3 和 4 之间、order 不许与骨架步骤撞号。

#### 🔴 四个钩子里，只有 `schema` 需要手写第二行

`decision.py` 里那句 `class KeeperDecision(HealthDecisionFields, DecisionModel)`
是唯一没法自动化的一步。用 `create_model` 动态拼基类可以省掉它，代价是
`decision.hp_changes` 在类型检查器眼里彻底消失——**整条链的类型安全为一行样板
买单，不划算**。所以选择：保留那一行，用测试兜住"注册了却忘了继承"。

漏了会**静默**：模型照常被教着写 `hp_changes`，而 schema 里没有这个字段，
pydantic 按 `extra="ignore"` 丢掉，血扣不上、日志里什么都没有。正是本项目
反复记的那类 bug，所以它必须有测试守着，不能靠自觉。

#### 切下去才发现的四件事

1. **漏了两个钩子，都是在真代码里撞出来的。** 原设计只有
   schema/prompt/executor。切到一半发现 `agent.py:383` 那行
   `format_npc_states` 无处安放——**能力不只要能改世界，还得让模型看见自己
   改成了什么样**（`exec/19 #39` 的原始症状），补成 `situation`。
   收尾复查时又发现 `logger.info("keeper_decision", ...)` 同样逐个列着各能力
   的字段，补成 `audit`。
   🔴 判据留给阶段 3：**编排层每一处"逐个列出各能力字段"的地方，都是一个还
   没被发现的钩子。** 切下一片之前先按这条 grep 一遍 `agent.py`。

2. **`resolve_npc_id` 不属于 health。** 它被 `world_state` 的
   `resolve_state_subject` 也用着。留在 health 里 = `world_state → health`，
   当场撞上"能力之间不许互相 import"。下沉成 `primitives/npcs.py`。
   **这是那条断言第一次在真代码里起作用**，而不是在设计稿上。

3. **能力边界不是每一处都落在文本边界上。** 裁决规则 3「理智/伤害」是
   san_check 与 health **共用的一句话**，切不开又不能改字面（改了就要重录
   磁带）。暂时留在骨架，等阶段 3 切 `san_check` 时连同改写一起做。如实记着，
   不假装规则 3 已经归位。

4. **能力要用的底座得先抽出来。** `adjust_hp_impl` 依赖 `KeeperDeps`、
   `resolve_character`、`write_stat`——都在 961 行的 `tools.py` 里。不抽
   `deps.py` 就等于让能力 import 那个大杂烩，**把待拆的耦合原样搬进新目录**。

#### 测试跟着能力走

`test_health_capability.py` 放在能力目录里（判据是"新人只读一个目录"，测试
是这个目录的一部分）。为此把 `tests/conftest.py` 挪到了 `trpg-backend/conftest.py`
——rootdir 的 conftest 才对 `app/` 下的用例生效。副作用：能力测试会被打进 wheel，
接受。

架构测试新增四条：能力之间不许互相 import、能力不许反向依赖编排层、原语不许
认识能力、`registry` 运行时必须是叶子。最后一条用了一个**只在那一条断言里生效**
的放宽——跳过 `if TYPE_CHECKING:` 块。理由写在代码里：那种 import 可证明不执行、
构不成 import 环，把它算进去 `registry` 就只能放弃精确类型标注，代价落在每个
写能力的人身上。**别处一律照旧，函数体内的延迟导入照样算。**

---

### 阶段 3 · 其余七个能力逐个切

`skill_check` / `san_check` / `movement` / `clue_reveal` / `agenda` /
`world_state` / `progression`。**每片独立，可并行**——这一步本身就在验证目标
是否达成。三处撞名的改名在对应片里顺手做。

### 阶段 4 · 拆 `agent.py` 剩下的部分（原阶段 2，推后）

先写表征测试锁死 if/elif 链的顺序语义，再拆成：主流程 / 分类与代码强制 /
两次 LLM 调用 / 结算 / 留痕。

🔴 **为什么推后**（第三次修正，同样由数据推动）：

1. **不必要**：实测 `agent.py` 里只有 3 处 health 相关代码，且全是边缘引用
   （局面块渲染、日志字段、prompt 文案），**切能力不需要先拆它**。
2. **它会自然变瘦**：46 次改动里相当一部分是能力相关引用，会被 registry 逐片
   收走。**等八片切完再看它还剩什么，那时拆得准**；现在拆是照当前形状拆，
   切完能力还得再动一次。
3. **风险顺序更合理**：先在低风险的 `health` 上验证机制，再动全项目最危险的
   文件。原顺序是反的。

代价：并行瓶颈（46/90）晚一点解除。但阶段 3 每切一片就松一点，不是堵到最后。

### 阶段 5 · 目录终态 + `ARCHITECTURE.md`

`core/` 顶层重排（COC7 规则、llm、基础设施分开），写一份新人入口文档：
每个目录是什么、加一个能力该动哪里、依赖方向为什么是这样。

## 六、每阶段的验证（不可省）

- 后端 `pytest` 全绿 + `ruff` + `ruff format` + `ty`；
- 前端/SDK `tsc` + `lint` + `build`；e2e 全量；
- **`test_architecture.py` 绿**；
- 阶段 3 起每阶段末重录磁带。

## 七、本轮明确**不做**

- **前端 `RoomPage.tsx`（1566 行）**：前端没有后端那种测试密度，风险不成比例。
  单独一轮，且要先补测试。
- **`turn_executor` 改注册表驱动**：留到阶段 2，届时是能力注册的自然结果，
  **不为此单独做一轮**——现在把 if/else 改成注册表而没有能力目录，是为抽象而抽象。
- **`ScenarioModule.visibility_pairs` 改名**：要迁移五个模组的 structured.json，
  代价不成比例，而"配对表"这个词本身不歧义。
- **业务行为的任何改变**：全程零功能变更。任何"顺手改好一点"都不许，
  否则测试红了分不清是重构错了还是改动错了。
