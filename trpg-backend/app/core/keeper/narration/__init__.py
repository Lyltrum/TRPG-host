"""给模型看的文本：局面块组装 · prompt 骨架 · 叙事纪律。

- `situation.py` —— 「局面块」组装器。裁决与分组叙事共用同一份，区别只在受众；
- `prompts.py` —— 两个 system prompt 的骨架 + 局面块渲染。裁决规则里**只剩
  流程类的那几条**（0/5/6/6b/7/11/12），能力自己的规则在各自目录里；
- `sheet_digest.py` —— 角色卡摘要（名单块里那一行）；
- `prose_discipline.py` —— **事后 scrub**：写完了再删/裁（长度硬顶、菜单式收尾、
  虚拟语气挡枪）；
- `narration_hints.py` —— **事前提醒**：写之前把话说死（检定边界、无待掷检定、
  动手未裁出检定、旁听者名单）。

🔴 后两个文件**整体都是概率性改进**（`exec/20`）：触发条件由代码确定性判断，
但模型服从与否是概率的。汇报时说"已改善（概率性）"，不说"已修复"。
"""
