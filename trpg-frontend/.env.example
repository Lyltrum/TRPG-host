# 后端地址。**默认不用配**：留空/注释掉时，前端从当前页面的主机名派生
# （`http://<当前 host>:8000/api/v1`，见 src/services/api-client.ts）。
#
# 🔴 局域网开局（朋友扫码进房）必须让它保持注释——钉成 127.0.0.1 的话，
# 朋友的手机会去请求它自己的 8000 端口。
#
# 只有后端跑在另一台机器/另一个端口时才需要显式指定：
# VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
