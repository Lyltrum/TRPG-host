"""编排与共享运行时状态：**能力无关**，不该随能力增长而变化。

判据一句话（贯穿 exec/27 阶段 3–4）：

> **共享的状态与它的读写归 runtime，用它做裁决的字段与执行归能力。**

- 编排：`agent.py`（主流程门面）· `turn_executor.py`（两条执行路径，纯由注册表
  驱动）· `heartbeat.py`（世界心跳）;
- 每轮的运行时：`deps.py`（依赖包与角色卡读写）· `turn_policy.py`（分类与代码
  强制，那条 if/elif **顺序即语义**）· `llm_calls.py`（三次模型往返的全部旋钮）
  · `decision_log.py`（裁决留痕）;
- 共享状态：`phase.py`（对局阶段——心跳/叙事长度/拒收行动都在读）·
  `location_state.py` + `scene_state.py`（谁在哪——叙事分组/讨论区投递/检定护栏
  都在读）· `pending.py`（两段式待掷队列，skill/san 共用的流程机制）;
- `tools.py` —— v1 自由工具的遗留残余，生产代码已不调用（见该文件说明）。

🔴 **能力不许 import 本包**（架构测试盯着）：加一片能力不该动这里一行。
"""
