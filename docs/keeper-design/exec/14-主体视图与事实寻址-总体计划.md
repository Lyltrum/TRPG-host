# 14：主体视图与事实寻址 —— 总体计划清单

> 这不是单个执行规格，是**一条线的分阶段清单**。每个阶段动手前另写细规格
> （沿用 exec/NN 编号）。设计结论来自 2026-07-30 的一轮长讨论，已经用户确认，
> 不需要重新论证方向。

## 一句话目标

把「可见性」从**挂在事实上的静态标签**，翻转成**挂在主体上的视图函数**
`view(subject) → facts`；主持人、玩家、NPC 是同一种东西，只是视图不同。

## 已确认的三条决策

**① 长战役与短模组不是二选一，是同一套机制的两种退化。**
分层记忆（L1 事实账本 / L2 分段摘要 / L3 最近窗口）在短模组下 L2 天然为空、
L3 覆盖全程，行为与现在完全一致。线上/线下同理：架构一致（per-observer 视图），
只有呈现层不同（同桌时旁人看得见屏幕 → 私密内容点击展开）。
**兼顾是默认，不是妥协**；真正需要排序的只是"先验证哪一种"。

**② 只有"能被泄露的东西"才需要 id。**
全量实体化会把世界变成查表，压死守秘人的即兴空间——这是 COC 调查的命脉。
所以采用**非对称寻址**：

| 内容 | 是否要 id | 受不受 view 管辖 |
|---|---|---|
| 线索、秘密、可揭开的信息条目 | ✅ 必须有 | ✅ 受管辖，泄密可断言 |
| 场景描写、氛围、即兴内容 | ❌ 不要 | ❌ 不受管辖，**但因此不许承载秘密** |

判据一句话：**秘密必须有 id；没有 id 的东西就只能是颜色。**

**③ schema 设计看全部 5 个模组归纳共性，迁移执行先跑 1 个验证管线。**
设计与铺开是两件事。N=1 设计 schema 已经踩过一次坑（`agenda.trigger`
三枚举从《追书人》一个模组倒推，后来退回自由文本，见 exec/04）。

## 两条贯穿全程的原则

- **权限的载体是"调用前给它的 schema"，不是"调用形式"。** tool call 和结构化
  JSON 在权限上同构；受限 schema 更强，因为越权动作**无法表达**（而不是被拦住）。
  → 不需要为了做权限回到 tool calling / agent 形状。
- **控制流保持 workflow。** 主体化 ≠ 自主化。Engine 决定何时问哪个主体，
  主体只在被问到时用自己的视图做**一次**决策。v1 的自由 loop 不复活。

---

## P0 · LLM 调用录制回放（安全网，先做）

后面 P1/P2 是这条分支上最大的一次重构，必须先有基线。

- [ ] 拦截 LLM 客户端，把 `(messages, params, response)` 落盘为 fixture
- [ ] replay 模式：从 fixture 读，测试进程不打网络
- [ ] 挑几段真实对局录成基线（覆盖：检定发起 / 理智链式 / 场景切换 / 迷茫引导）
- [ ] 把 exec/09、10、12 里有真实样本的问题转成回放用例

**验证**：同一 fixture 重放两次结果完全一致；断网状态下测试可跑。
**边界（写清楚免得高估）**：只保证"给定模型输出，代码行为不变"，
**不保证**"新 prompt 生成的内容更好"。它是代码的回归网，不是质量的评测器。

## P1 · 地基：事实寻址 + 元层切分

- [ ] **P1.1 归纳（读全部 5 个模组）**：确定"一条可泄露信息条目"的边界共性，
      产出 schema 草案 + 每模组预计条目数。**不要从单个模组倒推**
- [ ] **P1.2 schema 落地**：信息条目成为一等公民（id / 文本 / 所属节点 /
      揭开条件）；同时把**元层**字段显式标记
      （`kp_truth` `endings` `agenda` `core_clue_paths` `on_failure` `kp_notes`
      = meta；`public_text` / 节点描写 / NPC 公开身份 = diegetic）
- [ ] **P1.3 迁移**：先 1 个模组跑通全链路验证管线，通过后再批量其余 4 个
- [ ] **P1.4 校验扩展** `validate_module.py`：秘密必须有 id；元层字段不得出现在
      任何 diegetic 渲染路径里；引用闭合照旧

**验证**：5 个模组全过校验；keeper 真实冒烟跑通；P0 的回放用例不变红。

## P2 · 主体与权限（今天这个抽象的最小落地）

- [ ] `Subject` + `capabilities` 静态声明
- [ ] 按 capabilities 构造 DecisionSchema（KP = 全权，与现 `KeeperDecision` 等价）
- [ ] `turn_executor` 执行前校验 `intent ∈ capabilities`（纵深防御，schema 已限制过一次）
- [ ] `build_context(subject)`：上下文构造器按主体过滤（KP 传全集 = 与现状等价）
- [ ] 把 `agent.py` 里"运行时"与"KP 主体"的职责拆开（`turn_executor` 拆分 +
      L1-L5 那次整理已经走了两步，这里走完）

**验证**：KP 路径行为与 P0 基线**逐字一致**（重构不改行为，这是本阶段的核心断言）；
另构造一个受限 subject，断言它拿不到元层字段。

## P3 · 第一个语义守门人：泄密不变量

- [ ] 叙事生成时声明引用了哪些 fact id
- [ ] 断言引用集合 ⊆ `view(S)`
- [ ] 逐条判断 `prose_discipline.py` 里哪些事后正则可以退役（从"事后砍"变成
      "生成时就拿不到"），**不能退役的要说明为什么**

**验证**：变异检验——故意让一条未揭开的 fact 进入叙事，测试必须变红。

> 这是整个系统里语义轴上的第一个守门人。做完之后"泄密"从
> 不可验证的散文纪律问题，变成一条能变红的不变量。

## P4 · 分层记忆（长战役；短模组零影响）

- [ ] **L1 事实账本**：代码记账，谁 / 何时 / 通过什么手段 得到哪条 fact id
      （不靠 LLM 自觉写）
- [x] ~~`keeper_state` 上 schema~~ —— **核实后判定不需要做**：保留键早就有
      防写保护（`tools._RESERVED_STATE_KEYS` 覆盖全部 5 个键，`update_state_impl`
      会拒绝写入），剩下的自由键是**有意开放**给世界状态笔记的。给一个半开放的
      dict 上 schema 是没有价值的仪式。
- [ ] **L2 分段摘要**：session 边界 / 场景切换触发，**离线生成**，不在玩家等待路径上
- [ ] **L3 窗口**：沿用现有 `_HISTORY_LIMIT`；同时修掉 docstring 与实现的矛盾
      （现在写"重放完整历史"，实际是最近 200 条，超出静默丢开头）
- [ ] **上集回顾**：跨 session 重入时用 L1+L2 生成

**验证**：短模组下 L2 为空、行为与现在一致（**退化证明**）；
合成一局 300+ 事件的对局，断言第 3 轮的线索在第 200 轮仍可用。

## P5 · 多人 per-observer（线上/线下同构）

**P5.1 已完成**（`exec14-p5.1` / `7710c02`）：
- [x] **聚合窗口取代硬拒** —— 汇总发生在裁决之前，单人局窗口 0、逐字等价
- [x] ~~`CheckRequest.player` 改 id~~ —— **核实后不做**：`_resolve_character`
      已是白名单 + 找不到就列出房间里有谁，不是模糊匹配，收益小

**P5.2 已完成**（范围定稿见 `exec/18`），拆成四个子步各一个 tag：

- [x] **P5.2a** `exec14-p5.2a` —— **per-player 位置**（`location_state.py`）。
      `current_node_id` 语义收窄为「本轮**发言的人**共同到了哪」，新增 `moves`
      逐人覆盖；检定护栏与事实账本改按**掷骰那个人所在的节点**判定。
- [x] **P5.2b** `exec14-p5.2b` —— **per-observer 投递**。`ConnectionManager`
      记住连接属于谁 + `send_to_players`；`NarrationOutcome.segments`；
      玩家原话也按位置投递。房间只有一个位置组时走原路径、prompt 逐字不变。
- [x] **P5.2c** `exec14-p5.2c` —— ②潜行（常驻状态）+ ⑥玩家主动私密（一次性
      标记），共用同一套投递；前端加私密开关（每发一条自动复位）。
- [x] **P5.2d** `exec14-p5.2d` —— 🔴 **per-audience 上下文**。b/c 的保密只有
      投递是代码保证的，"别提另一组的事"还是靠 prompt。这一步把**历史 / 线索
      账本 / 本轮原话**三处按受众重建：门厅那段的模型上下文里根本没有地下室
      发生过什么。**保密从纪律变成结构**。
- [x] **聚光灯** —— 心跳选人从 `humans[0]` 改成「谁最久没说话」（从没说过话的
      排最前），超过 `_DEFAULT_SPOTLIGHT_SECONDS`（480s）强制注入引导。
- [x] ~~④ 递条子~~ —— **裁决不做**（靠模型自觉且无验证器，与 exec/17 同族）
- [x] 分组裁决方式 —— **已定：一次裁决全局、只叙事分开**（裁决器本就全知，
      私密只发生在输出侧；另两个方案要么延迟翻倍要么有等待感）

**P5.2 的残留缺口（如实记，不假装做完了）**：
1. **L2 分段摘要没有受众**，全房间共用一份。现在只用**公开历史行**去压摘要，
   代价是分头期间的剧情不进摘要。真解要给 chapter 也加受众。
2. **段内内容纪律仍是概率性的**：上下文已经裁干净，但"这段话有没有替另一组
   编造"这类语义问题依旧没有验证器。段尾的范围提示现在只承担文风。
3. **历史受众靠事件落库时记的 `audience`**：P5.2d 之前的老房间没有这个字段，
   一律视为公开——旧对局不会被追溯性地保密。

**P5.3 已完成**（`exec14-p5.3`）：
- [x] **⑨ 队友角色卡可见** —— 方向相反的放开。新增 `GET /rooms/{roomId}/characters`
      （鉴权只要求「你是房间成员」，不是「这张是你的」）+ `PartyCharacterRead`
      + SDK `listPartyCharacters` + 前端「房间成员」面板里点开看卡。
      ⑦⑧ 已定为公开，HP/SAN/技能全给；**只去掉建卡过程字段**
      （`generationMethod`/`attributePoolTotal`/`allocatedAttributes`/`notes`）
      ——那是卡主的建卡向导才需要的，队友看了没意义。
- [x] 线下同桌的呈现差异：私密内容点击展开（`NarrationSegment.covert` →
      `NarrationPushPayload.private` → 前端折叠成「只有你能看到 · 点按查看」）。
      线下旁人看得见你的屏幕，私密的物理前提本来就不成立，这是唯一补救。

## P6 · NPC 主体

P2 + P3 的自然产物：`view(NPC) = 虚构内它有权知道的 − 元层`。
注意 NPC 知道的**常常超过玩家**（教徒知道教团），缺的是元层不是信息量。
必须懒调用（被搭话/相关时才跑），否则延迟失控。

---

## 待办（已确认成立，不插队）

- [ ] **技能指向 id 化** —— 见 `exec/17`。停止用自由文本当标识符：模组数据
      组装期归一到 id 并硬失败（现有 43 条脏数据只报错不拦）、裁决输出改
      `skill_id` 白名单。同义词表是打地鼠，项目自己的 docstring 早就论证过。
      正解已有先例：`current_node_id`（07-30）。

## 随时可插的旁支（与主线不冲突）

- [ ] **CI 加 `feat/keeper-agent` 触发**（四个 workflow 现在只在 `main`/PR 上跑，
      这条分支零 CI）——半小时，能把现在手动跑的检查变成自动守门
- [ ] **前端 UI 视觉第二版**（限定作用域 + 结构性签名，见 `frontend-ui-polish`）——
      这是本条产品线的一部分，不是"另一条任务"

---

## 🔴 阶段台账 —— 真人实测出问题时怎么定位到阶段

这条线要连做 7 个阶段，中间用户会启动服务真人测。**每个阶段必须留下可回溯的锚点**：

| 纪律 | 做法 |
|---|---|
| 一个阶段 = 一个 commit | 不把两个阶段揉进一次提交 |
| 一个阶段 = 一个 tag | `git tag exec14-pN`，`git diff exec14-pN..exec14-pN+1` 立刻能看这阶段动了什么 |
| 一个阶段 = 一行台账 | 下表每完成一阶段追加一行，不回头补 |
| 每阶段收尾必跑磁带回放 | `pytest tests/test_keeper_replay.py`，行为变了当场知道 |

### 出问题时的定位顺序

1. **先分层**：症状属于叙事 / 裁决 / 可见性 / 记忆 / 前端 哪一层 → 查台账里哪个阶段碰过这层
2. **再回放**：在嫌疑阶段的 tag 上 `git stash` + checkout，跑同一条磁带用例，找**第一个行为变化的阶段**
3. **分清是代码还是模型**：磁带回放红 = 代码行为变了；磁带 drift 报告 = prompt 组装变了；两者都没红但真人测有问题 = **模型输出本身的波动**，不是回归（这条最容易误判成 bug）
4. 定位后按老规矩记进对应的 `NN-真人实测问题清单-*.md`

### 台账

| 阶段 | tag | commit | 动了哪一层 | 磁带 | 已知行为变化 |
|---|---|---|---|---|---|
| P0 录制回放 | `exec14-p0` | `3512f8e` | 仅 LLM 客户端外包一层（无磁带时纯透传） | 建立 `tests/tapes/keeper_minimal.json` | 无。KP 行为零改动 |
| P1.1 归纳 | —— | `37cd6d0` | 纯文档（exec/15） | —— | 无 |
| P1.2 schema | `exec14-p1.2` | `0998632` | IR（`module_loader`）+ 组装校验（`validate_module`） | 回放全绿 | **无**。`render_full` 未碰，facts 缺省为空，5 个真实模组零报错 |
| （插入）建卡修复 | —— | `68a6886` | 前端建卡提交路径（与本线无关，真人实测 exec/12 #30） | —— | 修好了：掷点池角色进游戏后衍生值/技能不再全 0 |
| P1.3 迁移 | `exec14-p1.3` | `84a3a91` | **模组数据**（`模组资料/*.structured.json`，5 个全迁）+ 迁移脚本 | 回放全绿 | **无**。`render_full` 逐字节相同，迁移对 keeper 不可见 |
| P3 泄密不变量 | `exec14-p3` | `0daa67e` | 叙事收尾（`_finalize_prose`，三条路径唯一咽喉）+ 新增 `leak_guard.py` | 回放全绿 | **有**（首次）：元层事实被逐字复述的句子会被丢弃。未迁移模组（facts 为空）完全无感 |
| P2 主体与权限 | `exec14-p2` | `3534985` | **首次碰运行时**：`turn_executor` 两个入口接 subject、`agent.py` 显式传 KEEPER、新增 `subject.py` 与 `render_for_subject` | 回放全绿 | **无**。全权限下 `build_decision_model`→`KeeperDecision` 本身、`sanitize` 原样返回同一对象、`render_for_subject(KP)`≡`render_full` |

| P4.1 线索账本 L1 | `exec14-p4.1` | `ebc1895` | 检定结算 + 局面块 + 待掷记录 | 回放全绿 | 有：多一个「已确认的线索」块（空则不渲染） |
| P4.2 分段摘要 L2 | `exec14-p4` | `9da31ab` | 场景切换处新增**后台** LLM 调用 + 局面块 | 回放全绿 | 有：多一个「前情提要」块（空则不渲染）；后台摘要不占玩家等待 |
| P5.1 收集窗口 | `exec14-p5.1` | `7710c02` | WS 提交路径（窗口取代硬拒）+ 新增 `turn_window.py` | 回放全绿 | 有（仅多人局）：窗口内提交合并成一轮。单人局窗口 0、逐字等价 |
| P5.2a per-player 位置 | `exec14-p5.2a` | `b012ec4` | 状态模型（新增 `location_state.py`）+ 裁决契约 `moves` + 检定护栏/账本按人判定 | drift（裁决 prompt 加 4b）→ **重录** | 有：护栏与账本改按掷骰者所在节点；未分头时判定结果不变 |
| P5.2b per-observer 投递 | `exec14-p5.2b` | `5aef962` | WS 投递层（`send_to_players`）+ `NarrationOutcome.segments` + 分组叙事 | 回放全绿 | 有（仅分头时）：一次裁决 N 段叙事。未分头 → 单段广播、prompt 逐字不变 |
| P5.2c 潜行 + 私密 | `exec14-p5.2c` | `f3456e9` | 裁决契约 `stealth` + `visibility=private` 落地 + **前端**私密开关 | drift（裁决 prompt 加 4c）→ **重录** | 有：`private` 不再回 NOT_IMPLEMENTED；隐匿者的行动不进别人那段 |
| P5.2d per-audience 上下文 | `exec14-p5.2d` | `4241167` | 历史/账本/原话三处按受众重建（`_visible_history`、`visible_fact_ids`、`NarrationContext.utterances`）+ 事件落库带 `audience` | 回放全绿（裁决 prompt 未动） | 有（仅分头时）：每段叙事的局面块只含该组该知道的。老数据无 `audience` → 一律公开 |
| P5.2 聚光灯 | 同上 | —— | 导演层选人（`heartbeat._pick_player`）+ `inject_spotlight_guidance` | 回放全绿 | 有（仅心跳轮）：选"最久没说话的人"而非 `humans[0]`；超 480s 强制注入引导 |
| P5.3 队友卡 | `exec14-p5.3` | —— | **REST 新端点** + DTO/codegen + SDK + **前端**成员面板与私密气泡折叠 | 回放全绿（keeper 未碰） | 有：新增 `GET /rooms/{id}/characters`；私密叙事在前端默认折叠 |

## 本计划不做

- 掷骰揭晓动画（已明确留作待办）
- 把主持人回合改回 agent / 自由 tool loop
- 全量实体化（物件、NPC 知识条目逐条 id 化）——见决策 ②
