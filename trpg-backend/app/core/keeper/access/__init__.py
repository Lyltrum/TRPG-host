"""`view(subject) → facts`：谁能看见什么、谁有权做什么（exec/14 P2）。

- `subject.py` —— 主体与能力集。**守秘人/NPC/玩家是同一种东西，只是视图与权限
  不同**；`build_decision_model` 让越权动作**无法表达**（字段不存在），
  `authorize_decision` 在执行边界再查一遍（纵深防御）；
- `leak_guard.py` —— 元层泄密守门：真相层被逐字复述就整句丢弃。

🔴 **权限的载体是"调用前给它的 schema"，不是调用形式。** 做权限不需要回到
agent 形状——控制流保持 workflow，主体只在被问到时用自己的视图做一次决策。

🔴 **保密靠「拿不到」，不是「请你别说」。** 判断一条保密是结构性还是纪律性，
只看模型的上下文里还有没有那条信息。
"""
