# 🔴 **这一份不能省，也不能只留仓库根那一份。**
#
# Docker 只读**构建上下文根目录**下的 `.dockerignore`。后端镜像的上下文是
# `./trpg-backend`（见 docker-compose.yml），所以仓库根那份对它完全无效。
# 第一次构建就因此把两样东西打进了镜像：
#   - `tapes/`  真实模组的磁带里常驻整份剧本正文 → **版权红线**
#   - `app.db`  本地开发库，里面是真实账号与对局记录
# 两样都不在 git 里，却差点随镜像分发出去。

# 版权：磁带含剧本正文
tapes/

# 本地开发数据库（含真实账号 / 对局记录），也不在 git 里
*.db
*.db-journal
*.db-shm
*.db-wal

# 密钥
.env
.env.*

# 装依赖会重建，拷进去只会拖慢构建；.venv 还是 macOS 平台的，进 Linux 容器无效
.venv/
**/__pycache__/
**/*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/

# JSON Schema 的临时导出产物
.schema-export/

# 测试与文档不参与运行
docs/
